<nav class="navbar navbar-expand-lg navbar-light bg-info">
  <div class="container-fluid">

  <div class="btn-group">
    <a href="/" class="btn  btn-info active">BLOGERAPP</a>
    <%if user_signed_in?%>
      <a href="/posts/new" class="btn active btn-info ">Create Post</a>
    <%end%>
  </div>
    <div class='rounded mr-lg-3'>
      <% if user_signed_in?%>
        <%= current_user&.email %>
        <%= link_to 'Log out', destroy_user_session_path, method: :delete, class: 'btn btn-danger rounded-pill shadow' %>
      <% else %>
        <%= link_to 'Log in', new_user_session_path, class: 'btn btn-success rounded-pill shadow' %>
      <% end %>
    </div> 
  </div>
</nav>